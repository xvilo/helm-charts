AirTrail has been deployed.

{{- if .Values.ingress.enabled }}

URL:
  http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}

Make sure `origin` in values matches this URL, otherwise form submissions
(login, registration) will be rejected.
{{- else }}

No ingress is enabled. Reach the app by port-forwarding:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "airtrail.fullname" . }} 3000:{{ .Values.service.port }}
  open http://localhost:3000
{{- end }}

{{- if .Values.database.existingSecret }}

Database URL is read from Secret "{{ .Values.database.existingSecret }}" (key "{{ .Values.database.existingSecretKey | default "db-url" }}").
{{- else }}

Database URL is stored in Secret "{{ include "airtrail.secretName" . }}" (key "db-url").
{{- end }}

The first account you register becomes the admin.
