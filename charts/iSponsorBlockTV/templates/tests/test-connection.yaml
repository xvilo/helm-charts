apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iSponsorBlockTV.fullname" . }}-test-connection"
  labels:
    {{- include "iSponsorBlockTV.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iSponsorBlockTV.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
