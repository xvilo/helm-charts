apiVersion: v1
kind: Service
metadata:
  name: {{ include "intel-gpu-exporter.fullname" . }}
  labels:
    {{- include "intel-gpu-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "intel-gpu-exporter.selectorLabels" . | nindent 4 }}
