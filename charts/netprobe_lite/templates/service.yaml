apiVersion: v1
kind: Service
metadata:
  name: {{ include "netprobe_lite.fullname" . }}
  labels:
    {{- include "netprobe_lite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netprobe_lite.selectorLabels" . | nindent 4 }}
