apiVersion: v2
name: symfony-app
description: A simple chart to deploy basic Symfony/PHP applications
type: application
version: 0.6.2
appVersion: 'main'
keywords:
  - php
  - symfony
  - application
sources:
  - https://github.com/xvilo/helm-charts/tree/main/charts/symfony-app
maintainers:
  - name: xvilo
    url: https://github.com/xvilo
annotations:
  # Supported kind: added, changed, deprecated, removed, fixed and security
  artifacthub.io/changes: |
    - kind: added
      description: "Added the ability to inject environment variables from a non-helm managed configmap."
    - kind: fixed
      description: "The configmap not correctly being used with consumer."
